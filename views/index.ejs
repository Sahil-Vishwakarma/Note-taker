<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <script
            src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
        <div class="bg-zinc-900 w-full min-h-screen text-white p-10">
            <form action="/create" method="post">
                <input
                    class="block outline-none bg-zinc-800 px-3 py-3 rounded-md w-full"
                    type="text"
                    placeholder="Title goes here"
                    name="title">
                <textarea
                    class="block resize-none outline-none mt-3 px-3 py-3 rounded-md w-full bg-zinc-800"
                    placeholder="Description goes here..."
                    name="details"></textarea>
                <input
                    class="text-blue-600 mt-3 px-3 py-3 bg-zinc-800 rounded-md"
                    type="submit"
                    value="Create Task">
            </form>
            <div class="tasks flex gap-3 mt-10 flex-wrap">
                <div class="task bg-zinc-800 w-72 rounded-md px-3 py-3">
                    <h1 class="text-3xl tracking-tighter">Some task here</h1>
                    <p class="text-xl font-light text-blue-500">Read more</p>
                </div>
            </div>
        </div>

    </body>
</html>